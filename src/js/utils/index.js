import BEM from './bem';
import toRange from './to-range';
import toStrokeStyles from './to-stroke-styles';
import useEventListener from './use-event-listener';

const utils = {
  BEM,
  toRange,
  toStrokeStyles,
  useEventListener,
};

export { BEM, toRange, toStrokeStyles, useEventListener, utils as default };
